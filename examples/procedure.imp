// demonstrates procedure definition and invocation

procedure fib(n; x) begin
    f0 := 0;
    f1 := 1;
    while k < n do
        t := f0;
        f0 := f1;
        f1 := f0 + t;
        k := k + 1
    end;
    x := f1
end;

fib(10; x);
print x
