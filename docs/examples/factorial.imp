// Procedure to compute the `n`-th factorial

procedure factorial(n; f) begin
    f := 1;
    while k <= n do
        k += 1;
        f *= k
    end
end;

for k := 0 to 20 do
    factorial(k; f);
    print f
end
